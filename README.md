### With NPM


```

// dev

$> npm install
$> npm run build-dev
$> npm run start-dev
```
```
// prod 

$> npm install
$> npm run build
$> npm run start
```

# Usage

Access the app at `localhost:8080/`
